# Upstage API Key — https://console.upstage.ai 에서 발급
UPSTAGE_API_KEY=up_xxxxxxxxxxxxxxxxxxxx

# 프론트엔드 Axios baseURL (로컬 개발: http://localhost:8000, 프로덕션: 빈 문자열)
VITE_API_BASE_URL=http://localhost:8000

# expenses.json 저장 경로 (Vercel 환경에서는 자동으로 /tmp/expenses.json 사용)
# DATA_FILE_PATH=backend/data/expenses.json
